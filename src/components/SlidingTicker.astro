---
const { slidingText, time } = Astro.props;
---

<div class="slidingTicker">
    <div class="tickerTrack" 
        style=`animation: ticker-scroll ${time}s linear infinite;`
        >
        <span>{slidingText}</span>
        <span>{slidingText}</span>
    </div>
</div>